//a JavaScript script that updates the text color of the <header> element to red (#FF0000):
// must use document.querySelector to select the HTML tag

const update_header = document.querySelector('header');

update_header.style.color = '#FF0000';
